"Cpp Boiler": {
		"prefix": "boiler",
		"body": [
			"#include <bits/stdc++.h>",
			"#define GETTER(type, var) type get##var() const { return var; }",
			"#define SETTER(type, var) void set##var(type val) { var = val; }",
			"#define nl (cout << endl)",
			"using namespace std;",
			"int main()",
			"{",
			"    $1",
			"    return 0;",
			"}"
		],
		"description": "Print a default code"
	}
